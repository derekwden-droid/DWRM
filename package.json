{
  "name": "dwrm",
  "version": "1.0.0",
  "private": true,
  "description": "DWRM — static site root. Frontend is prebuilt into /assets. API lives in /api (Python). No build step required at this level."
}
